<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="5dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="5dp" >

    <RelativeLayout
        android:id="@+id/circleRL"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginRight="5dp" >

        <ImageView
            android:padding="10dp"
            android:id="@+id/postThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/circle" />

        <TextView
            android:id="@+id/postThumbTextView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="A" />
    </RelativeLayout>

    <TextView
        android:id="@+id/postTitleLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/circleRL"
        android:layout_toRightOf="@id/circleRL"
        android:ellipsize="end"
        android:maxLines="2"
        android:text="This is a good Post, I hope it will have 2 lines. Please give me 2 lines"
        android:textIsSelectable="false"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/postDateLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/circleRL"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/circleRL"
        android:maxLines="1"
        android:text="April 12, 2013"
        android:textSize="12sp" />

</RelativeLayout>